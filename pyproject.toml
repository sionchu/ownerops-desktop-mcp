[project]
name = "ownerops-desktop-mcp"
version = "0.3.0"
description = "Remote Streamable HTTP gateway for the official Desktop Commander MCP engine"
requires-python = ">=3.12"
dependencies = [
  "mcp-stdio==0.43.6",
]

[dependency-groups]
dev = [
  "httpx>=0.28.0",
  "pytest>=9.0.0",
]
